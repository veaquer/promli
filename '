#pragma once

#include "core/extension_manager.h"
#include <string>
#include <vector>
namespace core {
class PromptEngine {
public:
  PromptEngine(ExtensionManager &ext_mgr);
  ~PromptEngine();
  std::string process_prompt(const std::string &prompt);
  std::vector<std::string> suggest_extensions();
};
} // namespace core
